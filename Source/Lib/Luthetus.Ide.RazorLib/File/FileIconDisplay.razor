@    using BlazorStudio.ClassLib.FileConstants
<span class="bstudio_solution-explorer-icon-display">
    @if (AbsoluteFilePath.IsDirectory)
    {
        if (AbsoluteFilePath.FileNameNoExtension == UniqueFileFacts.Properties)
        {
            <IconProperties/>
        }
        else if (AbsoluteFilePath.FileNameNoExtension == UniqueFileFacts.WwwRoot)
        {
            <IconWwwRoot/>
        }
        else
        {
            <IconFolder/>
        }
    }
    else
    {
        switch (AbsoluteFilePath.ExtensionNoPeriod)
        {
            case ExtensionNoPeriodFacts.DOT_NET_SOLUTION:
                <IconDotNetSolution/>
                break;
            case ExtensionNoPeriodFacts.C_SHARP_PROJECT:
                <IconCSharpProject/>
                break;
            case ExtensionNoPeriodFacts.C_SHARP_CLASS:
            case ExtensionNoPeriodFacts.RAZOR_CODEBEHIND:
                <IconCSharpClass/>
                break;
            case ExtensionNoPeriodFacts.CSHTML_CLASS:
                <IconCshtml/>
                break;
            case ExtensionNoPeriodFacts.RAZOR_MARKUP:
                <IconRazor/>
                break;
            case ExtensionNoPeriodFacts.JSON:
                <IconJson/>
                break;
            case ExtensionNoPeriodFacts.CSS:
                <IconCss/>
                break;
            case ExtensionNoPeriodFacts.JAVA_SCRIPT:
                <IconJs/>
                break;
            case ExtensionNoPeriodFacts.TYPE_SCRIPT:
                <IconTs/>
                break;
            default:
                <IconFile/>
                break;
        }
    }
</span>
