<div class="luth_te_widget-layer">
    @foreach (var widget in RenderBatch.ViewModel!.WidgetInlineList)
    {
        <div class="luth_te_widget">
            <DynamicComponent Type="widget.ComponentType"
                              Parameters="widget.ComponentParameterMap" />
        </div>
    }
    
    @foreach (var widget in RenderBatch.ViewModel!.WidgetInlineList)
    {
        <div class="luth_te_widget">
            <DynamicComponent Type="widget.ComponentType"
                              Parameters="widget.ComponentParameterMap" />
        </div>
    }
    
    @foreach (var widget in RenderBatch.ViewModel!.WidgetInlineList)
    {
        <div class="luth_te_widget">
            <DynamicComponent Type="widget.ComponentType"
                              Parameters="widget.ComponentParameterMap" />
        </div>
    }
</div>