@{ var headerType = TextEditorHeaderRegistry.GetHeader(_fileExtensionCurrent); }

@if (headerType is not null)
{
	<DynamicComponent Type="TextEditorHeaderRegistry.GetHeader(_fileExtensionCurrent)"
				  	Parameters="_componentInnerParameters"/>
}
else
{
	<TextEditorDefaultHeaderDisplay ComponentDataKey="ComponentDataKey"/>
}
